import './style.css'

console.log("Bookworm project is loaded. Ready for AI setup.");

// We will add the button listeners and fetch logic in a later step
// after the AI Assistant has been created and configured.